// ไฟล์นี้มีไว้สำหรับอัปโหลดขึ้น GitHub เป็นตัวอย่างเท่านั้น (Example Config)
// ไม่เก็บ Key จริงๆ ป้องกันการรั่วไหล
// เวลาเอาแอปไปใช้งาน ให้ก๊อปปี้ไฟล์นี้ เปลี่ยนชื่อเป็น config.js แล้วใส่ Key ของจริง

const CONFIG = {
    SUPABASE_URL: 'YOUR_SUPABASE_PROJECT_URL',
    SUPABASE_KEY: 'YOUR_SUPABASE_ANON_KEY'
};
